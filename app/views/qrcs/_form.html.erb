<%= form_for(@qrc) do |f| %>
  <% if @qrc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qrc.errors.count, "error") %> prohibited this qrc from being saved:</h2>

      <ul>
      <% @qrc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
